# Add your AWS account ID (you can get it with: aws sts get-caller-identity --query Account --output text)
aws_account_id = ""  # AWS account ID
db_password    = "db-password"
redis_password = "redis-password"
image_tag      = "latest"